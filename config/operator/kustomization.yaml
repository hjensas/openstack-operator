resources:
- ../crd/bases/operator.openstack.org_openstacks.yaml

bases:
#- ../crd/
- manifests
- rbac
- deployment
- ../certmanager/
- ../samples/

patchesStrategicMerge:
#- delete_crd.yaml

# Injects our custom images (ENV variable settings)
- manager_operator_images.yaml
- default_images.yaml
# Inject our custom manager leader election options (ENV variable settings)
# - manager_leader_election.yaml